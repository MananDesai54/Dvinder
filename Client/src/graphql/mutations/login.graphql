mutation Login($email: String!, $password: String!) {
  login(authData: { password: $password, email: $email }) {
    user {
      id
      email
      username
    }
    errors {
      field
      message
    }
    success
  }
}
