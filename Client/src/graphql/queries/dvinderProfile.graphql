query DvinderProfile($limit: Int!, $distance: Int) {
  dvinderProfile(limit: $limit, distance: $distance) {
    hasMore
    profiles {
      username
      profileUrl
      bio
      githubUsername
      birthDate
      flair
      feeds {
        title
        imageUrl
        code
        projectIdea
      }
    }
  }
}
